server:
  port: 8080

spring:
  application:
    name: backend-skeleton
  datasource:
    # 本地联调默认使用本机 MySQL；环境变量可覆盖
    url: ${DB_URL:jdbc:mysql://127.0.0.1:3306/skeleton?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
